# Industry Watcher — required env
# Copy to .env and fill in.

# OpenAI-compatible endpoint (cheap model is fine)
OPENAI_API_KEY=sk-...
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# Optional: Tavily for fallback when RSS is down
TAVILY_API_KEY=tvly-...
